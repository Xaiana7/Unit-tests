Исключения.
Чтобы протестировать исключения, нужно проверить:
что она его генерирует
объект, который передается через искл совпад с ожидаемым
значение совпадает с ожидаемым.

Искл это механизм, а передавать можно люб объект. Объект error просто объект с одним поолем - строковым (обычно сообщение об исклю-ии).
Когда проверяем на типизацию и налич неопр значений, надо проверять не то, что на входе массив-это массив, 
а то, что кажд элемент массива соотв всем требованиям (нет неопред знач, др типов значений)

опред знач
провер что это массив
длину массива
все знач определены, тот тип данных и проверять огранич, кот. должен удовл каким-то услов


Есть n контролируемых меропритяий. По каждому из них студент получил некоторую оценку. 
Нужно посчитать успешность освоения студентом какой-то дисциплины
Количество мероприятий 1<=n<=100
Оценка 0<=G(i)<=k(i)
1<=k(i)<=20 
U - ? вещественное число в процентах до 2х знаков после запятой средне-квадратичное
сумму K(i) - сумму всех возможных баллов
k(i) - целое 
G(i) - вещественное

Наследники класса Error
Исключния:
1. DeterminationError : "Determination Error!"
2. TypisationError : "Typisation Error!"
3. ArraySizeError : "Array X Error!"
4. ValueError : "Array X error"
X либо g либо k
e=0,01

Передается два массива G и K

Массивы должны быть одинакового размера